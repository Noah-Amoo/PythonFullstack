// Exercise One
const name = "Noah Amoo"

const sayHello = (name) => `Hello, ${name}`

const person = { name1: "Noah", age: 25}

const {name1, age} = person

console.log(person)

const numbers = [2, 4, 6]

console.log(numbers.map((n) => n * 2))